{
  /*'use client';

import { usePathname } from 'next/navigation';
import useScrollLogger from '@/hooks/useScrollLogger';

const ScrollLogger = () => {
  const pathname = usePathname();
  const screen_name = pathname.substring(1) || 'home';
  useScrollLogger(screen_name);

  return null; 
};

export default ScrollLogger;*/
}
